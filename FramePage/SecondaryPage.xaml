<Page
    x:Class="Social.FramePage.SecondaryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Social.FramePage"
    xmlns:uc="using:Social.Control"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:Social.Model"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <SolidColorBrush x:Key="BlueColor" Color="#FF30B3DD" />
    </Page.Resources>
    <Grid Loaded="Grid_Loaded">
        <uc:PostViewControl x:Name="Control"
            _CurrentPost="{x:Bind _CurrentPost}"
            _CurrentUser="{x:Bind _CurrentUser}"
            PostComments="{x:Bind PostComments}"/>
    </Grid>
</Page>
