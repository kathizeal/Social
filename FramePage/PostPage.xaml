<Page
    x:Class="Social.FramePage.PostPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Social.FramePage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:Social.Model" xmlns:local1="using:Social.Control"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    

    <Grid  >
        
        <NavigationView PaneDisplayMode="Auto" OpenPaneLength="120" Name="NavViewPostPage" 
                        SelectionChanged="NavViewPostPage_SelectionChanged"
                        Loaded="NavViewPostPage_Loaded"
                        IsBackEnabled="{Binding ElementName=PostFrame,Path=CanGoBack}"                        
                        >
            <NavigationView.MenuItems>
                
                    <NavigationViewItem Icon="Account" Content="Account" Tag="Account"/>
                <NavigationViewItem Icon="List" Content="List" Tag="List"/>

            </NavigationView.MenuItems>
            <Grid>
                <SplitView DisplayMode="Inline" IsPaneOpen="True" x:Name="MySpLitView">
                    <SplitView.Pane>
                        <StackPanel Background="#FF333333" x:Name="List" BorderBrush="Silver" BorderThickness="1">
                            <TextBlock Text="All List"/>
                            <ListView ItemsSource="{x:Bind PostList}" ItemClick="ListView_ItemClick" IsItemClickEnabled="True"  BorderBrush="LightSlateGray" BorderThickness="0.4" x:Name="ClickList" >
                                <ListView.ItemTemplate >

                                    <DataTemplate x:DataType="data:Post" >

                                        <TextBlock Text="{x:Bind PostContent}" FontSize="20" Foreground="White" HorizontalAlignment="Stretch" Margin="5,25,5,25"/>


                                    </DataTemplate>
                                </ListView.ItemTemplate>


                            </ListView>
                        </StackPanel>
                    </SplitView.Pane>
                    <SplitView.Content>
                        <Grid  x:Name="PostFrame" >
                            <Image Source="/Assets/Frontcover.jpg" Stretch="Fill"/>
                            <Frame x:Name="SecondFrame" Tag="SecondaryFrame" >

                            </Frame>
                        </Grid>

                    </SplitView.Content>
                </SplitView>
                
                
                
                
                
            </Grid>
        </NavigationView>
        <!--<VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="NarrowStateOnlyList">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                      
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="PostFrame.(Visibility)" Value="Collapsed"/>
                        <Setter Target="List.(Grid.ColumnSpan)" Value="2"/>

                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="NarrowStateOnlyFrame">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="List.(Visibility)" Value="Collapsed"/>
                        <Setter Target="PostFrame.(Grid.ColumnSpan)" Value="2"/>

                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="800"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                       
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>-->
    </Grid>
    
</Page>
